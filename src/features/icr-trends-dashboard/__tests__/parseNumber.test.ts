import { describe,it,expect } from 'vitest';import { parseNumber, parsePercentage, tokenNumbers } from '../logic/parseNumber';
describe('parseNumber',()=>{it('parses zero positives negatives decimals commas suffixes',()=>{expect(parseNumber('0').value).toBe(0);expect(parseNumber('-1,200.5').value).toBe(-1200.5);expect(parseNumber('2k').value).toBe(2000);expect(tokenNumbers('1 and 2')).toEqual([1,2]);});it('diagnoses malformed and percentages',()=>{expect(parseNumber('abc').ok).toBe(false);expect(parsePercentage('55%').value).toBe(.55);expect(parsePercentage('0.3').value).toBe(.3);});});
