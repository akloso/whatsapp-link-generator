import { describe,it,expect } from 'vitest';import { normalRow } from './fixtures';import { buildHealthDistribution, buildPortfolioSummary, buildRecommendationSummary, finiteDistribution } from '../logic/portfolioMetrics';
describe('portfolioMetrics',()=>{it('handles empty input without NaN or Infinity',()=>{const s=buildPortfolioSummary([]);expect(s.averageHealth).toBeNull();expect(s.ownerCoverage.percent).toBeNull();expect(Object.values(s).flat().every((v)=>typeof v!=='number'||Number.isFinite(v))).toBe(true);expect(finiteDistribution(buildHealthDistribution([]))).toBe(true);});it('summarises single and multiple account snapshots',()=>{const rows=[normalRow,{...normalRow,_clientKey:'beta',clientName:'Beta',rag:'Green' as const,health:96,recommendation:'Maintain cadence and validate next-quarter opportunity',owner:''}];const s=buildPortfolioSummary(rows,[{type:'invalid-date',issue:'x',resolution:'x'}]);expect(s.accountCount).toBe(2);expect(s.snapshotCount).toBe(2);expect(s.averageHealth).toBe(Math.round((normalRow.health+96)/2));expect(s.dataQualityIssueCount).toBe(1);expect(s.ownerCoverage.covered).toBe(1);expect(buildHealthDistribution(rows).Strong).toBe(1);const r=buildRecommendationSummary(rows);expect(r.total).toBe(2);expect(r.actionable).toBe(1);expect(r.informational).toBe(1);});});
