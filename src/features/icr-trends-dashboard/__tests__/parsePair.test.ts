import { describe,it,expect } from 'vitest';import { pairRatio, parsePair } from '../logic/parsePair';
describe('parsePair',()=>{it('parses pairs and division by zero',()=>{const p=parsePair('1937/119');expect(p.ok&&p.value.a).toBe(1937);expect(pairRatio(p.ok?p.value:null)).toBeCloseTo(1937/119);expect(pairRatio({a:1,b:0,count:2,ambiguous:false})).toBeNull();});it('diagnoses missing sides whitespace malformed and ambiguous tokens',()=>{expect(parsePair(' 10 ').ok).toBe(true);expect(parsePair('abc').ok).toBe(false);const p=parsePair('1/2/3');expect(p.ok&&p.value.ambiguous).toBe(true);});});
