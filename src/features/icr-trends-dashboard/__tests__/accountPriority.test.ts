import { describe,it,expect } from 'vitest';import { normalRow } from './fixtures';import { buildAccountPriorityRows, sortPriorityRows } from '../logic/accountPriority';
describe('accountPriority',()=>{it('ranks critical actionable accounts before healthy informational accounts',()=>{const healthy={...normalRow,_row:3,_clientKey:'b',clientName:'Beta',rag:'Green' as const,health:95,recommendation:'Maintain cadence and validate next-quarter opportunity'};const rows=buildAccountPriorityRows([healthy,normalRow]);expect(rows[0].clientName).toBe('Acme College');expect(rows[0].actionable).toBe(true);expect(rows[1].category).toBe('Strong');});it('sorts deterministically with missing score/date/ticket fields',()=>{const a={...normalRow,_row:5,_clientKey:'a',clientName:'Alpha',timestamp:null,tickets:null,health:50};const b={...a,_row:6,_clientKey:'b',clientName:'Beta'};const one=buildAccountPriorityRows([b,a]);const two=buildAccountPriorityRows([b,a]);expect(one.map(r=>r.clientName)).toEqual(two.map(r=>r.clientName));expect(sortPriorityRows(one,{key:'clientName',direction:'desc'})[0].clientName).toBe('Beta');});});
