import { describe,it,expect } from 'vitest';import { normalRow } from './fixtures';import { activeFilterCount, applyReportFilters, clearReportFilters, deriveReportFilterOptions, reconcileReportFilters } from '../logic/reportSelectors';
const rows=[normalRow,{...normalRow,_row:3,_clientKey:'beta',clientName:'Beta',rag:'Green' as const,owner:'Owner B',trackingStatus:'Placed',timestamp:new Date(Date.UTC(2026,2,1)),recommendation:'Maintain cadence and validate next-quarter opportunity'}];
describe('reportSelectors',()=>{it('applies no, single, combined, search and date filters',()=>{expect(applyReportFilters(rows,{latestOnly:false})).toHaveLength(2);expect(applyReportFilters(rows,{owner:'Owner B',latestOnly:false})).toHaveLength(1);expect(applyReportFilters(rows,{rag:'Green',owner:'Owner B',latestOnly:false})).toHaveLength(1);expect(applyReportFilters(rows,{search:'acme',latestOnly:false})[0].clientName).toBe('Acme College');expect(applyReportFilters(rows,{from:new Date(Date.UTC(2026,2,1)),to:new Date(Date.UTC(2026,2,1)),latestOnly:false})).toHaveLength(1);expect(applyReportFilters(rows,{owner:'Missing',latestOnly:false})).toHaveLength(0);});it('derives stable options and reconciles workbook replacements',()=>{const o=deriveReportFilterOptions(rows);expect(o.clients.map(c=>c.label)).toEqual(['Acme College','Beta']);expect(o.owners).toEqual(['Owner A','Owner B']);expect(o.statuses).toContain('Placed');expect(activeFilterCount({owner:'Owner A',search:'x',latestOnly:true})).toBe(2);expect(clearReportFilters()).toEqual({latestOnly:true});expect(reconcileReportFilters({owner:'Gone',clientKey:'missing'},o).owner).toBeUndefined();});});
