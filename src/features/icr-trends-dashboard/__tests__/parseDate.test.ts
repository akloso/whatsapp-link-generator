import { describe,it,expect } from 'vitest';import { parseDate } from '../logic/parseDate';
describe('parseDate',()=>{it('parses timezone-safe ISO slash and Date inputs',()=>{expect(parseDate('2024-02-29').ok).toBe(true);expect(parseDate('1/15/2026 10:00:00').ok).toBe(true);expect(parseDate(new Date(Date.UTC(2026,0,1))).ok).toBe(true);});it('rejects impossible empty invalid and serial boundary',()=>{expect(parseDate('2023-02-29').ok).toBe(false);expect(parseDate('').ok).toBe(false);expect(parseDate('wat').ok).toBe(false);expect(parseDate(0).ok).toBe(false);expect(parseDate(25569).ok).toBe(true);});});
