import { describe,it,expect } from 'vitest';import { headerMatchScore, matchHeaders, similarity } from '../logic/headerMatching';
describe('headerMatching',()=>{it('matches exact labels aliases and duplicate canonical targets',()=>{const m=matchHeaders(['Client Name','organisation','RAG Status','foo']);expect(m['Client Name'].key).toBe('clientName');expect(['duplicate','ignore']).toContain(m.organisation.status);expect(m['RAG Status'].key).toBe('rag');expect(m.foo.status).toBe('ignore');});it('scores fuzzy deterministically and handles sparse non-string headers',()=>{const a=matchHeaders(['customer success owner',null,42,'account health']);const b=matchHeaders(['customer success owner',null,42,'account health']);expect(a).toEqual(b);expect(a['customer success owner'].key).toBe('owner');expect(headerMatchScore('lead capacity','lead capacity')).toBeGreaterThan(.6);expect(similarity('alpha beta','alpha gamma')).toBeGreaterThan(0);});it('flags ambiguous near matches',()=>{const m=matchHeaders(['status']);expect(['ambiguous','ignore','review','auto']).toContain(m.status?.status);});});
