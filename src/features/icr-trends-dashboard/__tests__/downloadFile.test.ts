import { describe,it,expect,vi } from 'vitest';import { downloadBlob } from '../services/downloadFile';
describe('downloadFile',()=>{it('creates clicks removes and revokes object URLs',()=>{const click=vi.fn();const remove=vi.fn();const append=vi.fn();const anchor={style:{},click,remove};vi.stubGlobal('URL',{createObjectURL:vi.fn(()=> 'blob:x'),revokeObjectURL:vi.fn()});vi.stubGlobal('document',{createElement:vi.fn(()=>anchor),body:{appendChild:append}});downloadBlob(new Blob(['x']),'safe.csv');expect(append).toHaveBeenCalledWith(anchor);expect(click).toHaveBeenCalled();expect(remove).toHaveBeenCalled();expect(URL.revokeObjectURL).toHaveBeenCalledWith('blob:x');vi.unstubAllGlobals();});});
