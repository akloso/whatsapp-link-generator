import { describe,it,expect } from 'vitest';import { exportCell, safeFilename, safeText, safeWorksheetName } from '../logic/exportSanitizers';
describe('exportSanitizers',()=>{it('neutralizes dangerous text but preserves typed primitives',()=>{expect(safeText('=SUM(A1:A2)')).toBe("'=SUM(A1:A2)");expect(safeText('  +CMD')).toBe("'  +CMD");expect(safeText('-10+20')).toBe("'-10+20");expect(safeText('@IMPORT')).toBe("'@IMPORT");expect(exportCell(-5)).toBe(-5);expect(exportCell(0)).toBe(0);expect(exportCell(false)).toBe(false);expect(exportCell(null)).toBe('');});it('sanitizes filenames and worksheet names deterministically',()=>{expect(safeFilename('../bad\u0000/name   .')).toBe('.. bad name');expect(safeFilename('')).toBe('zapora-icr-report');const used=new Set<string>();expect(safeWorksheetName('Bad:/Name*With?LongLongLongLongLongLong',used).length).toBeLessThanOrEqual(31);expect(safeWorksheetName('Bad:/Name*With?LongLongLongLongLongLong',used)).not.toBe([...used][0]);});});
