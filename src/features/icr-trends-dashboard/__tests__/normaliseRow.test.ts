import { describe,it,expect } from 'vitest';import { mapping, rawRow } from './fixtures';import { normalizeRag, normaliseRow, normaliseRows } from '../logic/normaliseRow';
describe('normaliseRow',()=>{it('normalises valid row without mutating frozen input',()=>{const out=normaliseRow(rawRow,mapping,2);expect(out.row?.clientName).toBe('Acme College');expect(out.row?.rag).toBe('Red');expect(Object.isFrozen(rawRow)).toBe(true);expect(normaliseRow(rawRow,mapping,2)).toEqual(out);});it('collects missing invalid and unknown statuses',()=>{const out=normaliseRows([{'Client Name':'','RAG analysis (Based on CS understanding)':'Blue','Timestamp':'bad','Zendesk Tickets':'abc'}],mapping);expect(out.rows[0].rag).toBe('Unknown');expect(out.issues.some(i=>i.type==='missing-required-field')).toBe(true);expect(out.issues.some(i=>i.type==='invalid-date')).toBe(true);expect(normalizeRag('yellow')).toBe('Amber');});});
