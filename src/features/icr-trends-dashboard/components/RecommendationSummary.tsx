import type { RecommendationSummary as Summary } from '../types/report';
export function RecommendationSummary({summary}:{summary:Summary}){ return <section className="icr-dashboard__panel-lite" aria-labelledby="recommendation-summary-title"><h2 id="recommendation-summary-title">Recommendation summary</h2><p>{summary.actionable} actionable · {summary.informational} informational · {summary.total} total.</p><ul className="icr-dashboard__summary-list">{summary.themes.map((t)=><li key={t.label}><span>{t.label}</span><strong>{t.count}</strong></li>)}{summary.total===0?<li>No recommendation themes in the current result set.</li>:null}</ul></section>; }
