import type { IcrWorkbookState } from '../hooks/useIcrWorkbook';
export function ParsedDataSummary({state}:{state:IcrWorkbookState}){ const worksheet=state.candidates.find((c)=>c.index===state.selectedWorksheetIndex); return <div className="icr-dashboard__summary"><h2 id="icr-dashboard-workspace-title">Parsed workbook summary</h2><div className="icr-dashboard__summary-grid"><strong>{state.rows.length}<span>parsed rows</span></strong><strong>{state.skippedRows}<span>skipped empty rows</span></strong><strong>{state.issues.length}<span>quality issues</span></strong><strong>{state.mappedColumnCount}<span>mapped fields</span></strong></div><p>Worksheet: <strong>{worksheet?.name}</strong>. Reporting dashboards, charts, exports, and persistence are reserved for later phases.</p></div> }
